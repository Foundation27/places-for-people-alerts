import os\nimport smtplib\n\ndef send_test_email():\n    sender_email = os.getenv('SMTP_USER')\n    sender_password = os.getenv('SMTP_PASSWORD')\n    recipient_email = 'test@example.com'  # Change to the recipient email address\n\n    message = 'Subject: Test Email\n\nThis is a test email sent from the test_email.py script.'\n\n    try:\n        with smtplib.SMTP('smtp.example.com', 587) as server:  # Change to your SMTP server\n            server.starttls()  # Secure the connection\n            server.login(sender_email, sender_password)\n            server.sendmail(sender_email, recipient_email, message)\n        print('Test email sent successfully!')\n    except Exception as e:\n        print(f'Error: {e}')\n\nif __name__ == '__main__':\n    send_test_email()